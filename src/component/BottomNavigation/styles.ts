import styled from '@emotion/styled';

import { BottomStyleProps } from './type';
export const BottomStyle = styled.div<BottomStyleProps>`
  /*
    BaseBottomNav Style
  */
  display: flex;
  justify-content: center;
  gap: 40px;
  font-size: 30px;
`;
